import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)

// Things I must do ....

// -- Using Zustand or context instead of props

// Things I have Done ....

// ++ Adding User Values in a list 
// ++ Show them in a list and give them an Order with numbers 
// ++ Adding delete button to each line of the list for deleting them from list by pressing the button
// ++ Add a simple UI to the lists 